namespace Halfblood.UnitTests.TrackerDataGridTests
{
    using System;

    using Rhino.Mocks;

    public class TrackerDataGridTest
    {
        public void Test()
        {
            //var tracker = MockRepository.GenerateMock<ITracker>();
            //IDisposable disposable = tracker.Track(dataGrid, );
        }
    }
}
