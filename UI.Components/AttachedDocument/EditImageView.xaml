<UserControl x:Class="UI.Components.AttachedDocument.EditImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:attachedDocumentDomain="clr-namespace:UI.Infrastructure.ViewModels.AttachedDocumentDomain;assembly=UI.Infrastructure"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance attachedDocumentDomain:IEditImageViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Orientation="Horizontal">
            <Button Command="{Binding Path=RotateCommand}" Content="повернуть" />
        </StackPanel>
    </Grid>
</UserControl>
