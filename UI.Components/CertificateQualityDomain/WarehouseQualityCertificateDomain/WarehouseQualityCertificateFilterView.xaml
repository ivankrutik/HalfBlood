<UserControl x:Class="UI.Components.CertificateQualityDomain.WarehouseQualityCertificateDomain.WarehouseQualityCertificateFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:filters="clr-namespace:UI.Infrastructure.ViewModels.Filters;assembly=UI.Infrastructure"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance filters:IWarehouseQualityCertificateFilterViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button x:Name="SearchButton"
                Margin="2,2,0,0"
                Content="поиск" />

        <TextBlock Grid.Row="1"
                   Margin="2,2,0,0"
                   Text="Материал" />

        <TextBox x:Name="FullRepresentationTextBox"
                 Grid.Row="2"
                 Margin="2,2,0,0" />
    </Grid>
</UserControl>
